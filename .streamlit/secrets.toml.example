# Example secrets file - copy to secrets.toml and add your actual API key
GOOGLE_API_KEY = "your_google_gemini_api_key_here"

# For production deployment, add this to your Streamlit Cloud secrets
# Do not commit the actual secrets.toml file to version control
